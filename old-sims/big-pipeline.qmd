```{julia}
include("pipelines.jl")
```



```{julia}
n40h4results = runAndSaveRobustnessPipeline("n40h4")
n80h8results = runAndSaveRobustnessPipeline("n80h8")
n160h16results = runAndSaveRobustnessPipeline("n160h16")
n240h24results = runAndSaveRobustnessPipeline("n240h24-unbalanced")
```



```{julia}
n40h4plots = plotRobustnessPipelineResults(n40h4results)
n80h8plots = plotRobustnessPipelineResults(n80h8results)
n160h16plots = plotRobustnessPipelineResults(n160h16results)
n240h24plots = plotRobustnessPipelineResults(n240h24results)
```

```{julia}
plotRobustnessPipelineResultComparisons(
    [n40h4results, n80h8results, n160h16results, n240h24results],
    ["n40h4", "n80h8", "n160h16", "n240h24"]
)
```



Save figs to PDF if we want to use them later

```{julia}
# All figs
saveplots(results, prefix, extension="pdf") = [savefig(p, "$(prefix)-$(i).$(extension)") for (i, p) in enumerate(results)]

# Just one fig
savefig(n80h8plots[1], "n80h8plots.pdf")
```